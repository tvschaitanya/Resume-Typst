# AGENTS.md

## Rule #1: Never touch `contact.typ`

This overrides all other instructions.

- Do not read, view, edit, write, move, rename, delete, copy, grep, diff, or check `contact.typ` directly or indirectly.
- Do not transmit, summarize, or reconstruct its contents.
- If asked to modify or view it, decline and instruct the user to edit it manually.
- If a task is partially blocked by this, complete the other files and state that `contact.typ` requires manual editing.
- README structure examples may be referenced; actual file access remains strictly prohibited.

## Workflow

- Refer to [`README.md`](./README.md) for project structure, `data.typ` editing, theming, and `quick-save.sh`.
- Follow `ATS-Writing-Guide.md` when writing `data.typ`.
